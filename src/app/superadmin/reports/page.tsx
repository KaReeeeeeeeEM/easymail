import { count, eq } from "drizzle-orm";
import { Activity, MailCheck, MailX, Server } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { db } from "@/db";
import { emailDelivery, smtpConfiguration } from "@/db/schema";
export default async function ReportsPage() { const [[total], [sent], [failed], [senders]] = await Promise.all([db.select({ value: count() }).from(emailDelivery), db.select({ value: count() }).from(emailDelivery).where(eq(emailDelivery.status, "sent")), db.select({ value: count() }).from(emailDelivery).where(eq(emailDelivery.status, "failed")), db.select({ value: count() }).from(smtpConfiguration)]); const reports = [{ label: "All requests", value: total.value, icon: Activity }, { label: "SMTP accepted", value: sent.value, icon: MailCheck }, { label: "Failed", value: failed.value, icon: MailX }, { label: "Configured senders", value: senders.value, icon: Server }]; return <div className="flex flex-col gap-8"><div><p className="text-sm font-medium text-primary">Managerial oversight</p><h1 className="mt-2 text-3xl font-semibold">Reports</h1><p className="mt-2 text-muted-foreground">Platform-wide delivery and infrastructure reporting.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{reports.map(({ label, value, icon: Icon }) => <Card key={label}><CardHeader className="flex flex-row items-center justify-between"><CardDescription>{label}</CardDescription><Icon className="text-primary" /></CardHeader><CardContent><p className="text-3xl font-semibold">{value}</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Delivery posture</CardTitle><CardDescription>SMTP acceptance is provider acknowledgement, not inbox placement.</CardDescription></CardHeader><CardContent><p className="text-4xl font-semibold text-primary">{total.value ? Math.round((sent.value / total.value) * 1000) / 10 : 0}%</p><p className="mt-2 text-sm text-muted-foreground">Platform acceptance rate</p></CardContent></Card></div>; }
